# 避難所検索画面を実装するための一括プロンプト

あなたは既存システムを改修するシニアWebエンジニアです。以下の要件をすべて満たす避難所検索画面を、ユーザーへの追加質問なしで実装してください。まず既存のルーティング、共通レイアウト、避難所データ構造、検索結果画面を確認し、現在の技術スタックと命名規則に合わせてください。既存機能や既存データを壊さず、変更範囲を避難所検索機能に限定してください。

## 実装する画面

避難所検索画面を作成または改修してください。画面上部には次の4つのナビゲーションボタンを表示してください。

- ホーム
- 避難所検索
- 避難所登録
- 指示ボード

ナビゲーションは既存の共通レイアウトに統合してください。画面幅が狭い場合はハンバーガーボタンを表示し、クリックでナビゲーションを開閉できるようにしてください。開閉状態は `aria-expanded` などのアクセシビリティ属性にも反映してください。

## 検索画面のレイアウト

避難所検索画面は、デスクトップでは左右2分割にしてください。

- 左半分: 検索フォーム
- 右半分: 避難所マップ

画面幅が狭い場合は、左のフォームを上、右のマップを下にして1列で表示してください。フォームとマップには、既存デザインに合う適度な余白、枠線、角丸、影を設定してください。

## 左側の検索フォーム

### 1. 位置情報

「現在地を取得」または「位置情報を許可」ボタンを配置してください。

ブラウザの Geolocation API を使い、次の動作を実装してください。

- 位置情報が利用できない場合は、利用できない旨を表示する
- 位置情報を取得中であることを表示する
- 許可されなかった場合は、その旨を表示する
- 取得した緯度・経度をフォームの hidden input に保持する
- 取得した位置をマップの中心に移動する
- 現在地にマーカーを表示する
- 再取得時は古い現在地マーカーを削除する

位置情報を取得できなくても、避難所検索自体は利用できるようにしてください。

### 2. フリー検索

避難所名、住所、説明などを入力できるフリー検索欄を追加してください。

- `search` 型または同等の入力欄を使う
- 空欄でも検索できる
- 検索語を避難所データの名称、住所、説明などの検索対象文字列と照合する
- GETパラメータなど、既存システムに合う方法で検索条件を送信する

### 3. 避難したい人数

「避難したい人数」の数値入力欄を追加してください。

- 1以上の整数を入力できる
- 数値入力用のキーボードが表示されるようにする
- 入力値を検索リクエストに保持する
- 検索結果画面に「避難予定人数: ○人」のように表示する
- 避難所データに定員または収容人数の項目が存在する場合は、入力人数を収容できる避難所に絞り込む
- 定員項目が存在しない場合は、人数条件を保持・表示し、誤った定員判定は行わない

### 4. 必要な設備

チェックボックスで複数選択できる設備条件を追加してください。

- ペット可
- バリアフリーあり
- 未就学児
- 駐車場
- 高齢者への配慮
- 障害のある方への配慮

複数選択された場合は、原則として選択したすべての条件を満たす避難所だけを表示してください。既存の避難所データで使われているキー名を優先し、必要に応じて次のような表記ゆれにも対応してください。

- ペット: `pet`, `pet_allowed`, `pets_allowed`, `ペット可`
- バリアフリー: `barrier_free`, `barrierFree`, `バリアフリー`
- 未就学児: `preschool`, `preschool_children`, `未就学児`
- 駐車場: `parking`, `parking_available`, `駐車場`
- 高齢者: `elderly`, `elderly_consideration`, `高齢者`, `高齢者への配慮`
- 障害のある方: `disability`, `disability_consideration`, `障害者`, `障害のある方への配慮`

真偽値、`true`、`可`、`あり` など、既存データで採用されている値の形式にも合わせてください。

### 5. 対応する災害

チェックボックスで複数選択できる災害条件を追加してください。

- 地震
- 洪水
- 土砂崩れ

設備条件と同様に、選択した条件をすべて満たす避難所を絞り込んでください。既存データのキー名を優先し、必要に応じて次のキー名を扱ってください。

- 地震: `earthquake`, `earthquake_safe`, `地震`
- 洪水: `flood`, `flood_safe`, `洪水`
- 土砂崩れ: `landslide`, `landslide_safe`, `土砂崩れ`

### 6. 検索実行

フォーム下部に「条件を指定して検索」ボタンを配置してください。

検索実行時は、次の条件をリクエストに引き継いでください。

- フリー検索語
- 避難したい人数
- 選択した設備条件
- 選択した災害条件
- 取得済みの緯度・経度

## 右側の避難所マップ

Leaflet または既存システムで採用している地図ライブラリを使ってください。既存システムに地図ライブラリがない場合は、Leaflet 1.9.4 と OpenStreetMap タイルを使用してください。

実装要件:

- 地図を右半分のマップパネルに表示する
- アプリの対象地域または既存設定に合わせた初期中心位置を設定する
- 初期ズームを設定する
- OpenStreetMap の帰属表示を付ける
- 位置情報取得後は現在地へ移動する
- 現在地マーカーを表示する
- 地図が表示されるための高さを固定し、画面幅に応じて調整する
- 既存の Leaflet 地図がある場合は、同じライブラリを二重初期化しない

避難所データに緯度・経度が存在する場合は、避難所マーカーも表示し、避難所名をポップアップで表示してください。緯度・経度がない場合は、無理に推測したマーカーを作らないでください。

## 検索結果

既存の検索結果画面を利用できる場合は再利用してください。新規に作る場合は次を満たしてください。

- 検索結果の件数を表示する
- 避難所名を表示する
- 住所があれば表示する
- 入力した避難予定人数を表示する
- 条件に一致する避難所がない場合は、該当なしメッセージを表示する
- 検索画面へ戻るリンクを表示する
- 既存のエスケープ処理やテンプレートの安全な出力方法を使う

## データとバックエンド

既存の検索APIや検索ルートがある場合は、それを拡張してください。新しく作る場合は、画面のフォームから送られた条件を受け取り、検索結果を返すルートを追加してください。

検索ロジックは次の方針にしてください。

1. 既存の地区などの検索条件を壊さない
2. フリー検索は入力がある場合だけ適用する
3. 設備条件は選択がある場合だけ適用する
4. 災害条件は選択がある場合だけ適用する
5. 複数の設備・災害条件はすべて満たすものを残す
6. 既存データにない設備条件は、該当データがない限り一致扱いにしない
7. 検索条件を結果画面に引き継ぐ
8. 不正な人数値は安全に扱い、アプリをエラー終了させない

## 見た目とアクセシビリティ

- 日本語UIにする
- 入力欄には対応する `label` を付ける
- 設備条件と災害条件は `fieldset` と `legend` を使う
- ボタンの文字は画面上で読めるようにする
- 位置情報の状態メッセージは `aria-live` を使う
- キーボード操作でフォームを利用できるようにする
- スマートフォンでも文字やボタンがはみ出さないようにする
- 既存システムの色、フォント、余白、ナビゲーションを尊重する
- 不要なページ全体の作り直しや無関係なリファクタリングは行わない

## 実装後の確認

実装後、次を自動または手動で確認してください。

- アプリが起動する
- 避難所検索画面がHTTP 200で表示される
- 上部にホーム、避難所検索、避難所登録、指示ボードが表示される
- ハンバーガーボタンでナビゲーションを開閉できる
- フリー検索欄が表示される
- 避難したい人数の入力欄が表示される
- 6つの設備チェックボックスが表示される
- 3つの災害チェックボックスが表示される
- 現在地取得ボタンが表示される
- Leaflet地図が空白にならず表示される
- 検索条件を送信できる
- フリー検索で該当する避難所を絞り込める
- 条件に一致しない場合に空状態が表示される
- デスクトップとスマートフォンの両方でレイアウトが崩れない
- 既存画面の主要機能が壊れていない

実装を完了したら、変更したファイル、実装内容、実行した確認コマンド、未対応の前提条件を簡潔に報告してください。質問で止まらず、既存コードから合理的に判断して最後まで実装してください。
